package dialight.autorespawn;

import org.bukkit.event.Listener;

public class AutoRespawnListener implements Listener {

    private final AutoRespawn proj;

    public AutoRespawnListener(AutoRespawn proj) {
        this.proj = proj;
    }

}
