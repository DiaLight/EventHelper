name: ToolLib
version: @version@
main: dialight.toollib.ToolLibPlugin
api-version: 1.13
authors: [DiaLight]
description: Useful tool to help event masters with theirs job
depend: [EventHelper]

commands:
  tool:
    description: Command to control all registered tools
    permission: toollib.tool
    usage: /<command>
